# op 写手培训入口（可选）

先读取 `../../core/write-consumer-comments/SKILL.md`，再根据产品读取 `../../products/dji/` 对应入口。该适配只说明路由，不复制共享写法，也不依赖特定 AI 工具。

所有写评、改写和案例演示均按 `training_fiction` 处理；原帖证据、产品事实和人物设定分开保存。横测必须以任务指定产品为种草中心。
