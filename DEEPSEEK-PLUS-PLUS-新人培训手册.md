# 阿豹追猎总控：DeepSeek++ 新人培训手册

本手册用于从零安装 DeepSeek++，导入 GitHub 仓库中的“阿豹追猎总控”，并按统一流程完成资料读取、评论培训、全批去重和最终质检。

仓库地址：<https://github.com/ly23055601-glitch/desktop-tutorial1>

总控技能直达地址：<https://github.com/ly23055601-glitch/desktop-tutorial1/blob/main/deepseek/abao-controller/SKILL.md>

DeepSeek++ 是面向 DeepSeek 网页版的社区浏览器扩展。它的 Skill 页面支持从 GitHub 仓库、目录或单个 `SKILL.md` 导入技能；本手册按其公开安装与 Skill 功能编写。使用前应以扩展当前版本的界面为准：<https://github.com/zhu1090093659/deepseek-pp/wiki/Installation>。

---

## 一、先理解这套包是怎么工作的

仓库中保留原始资料、产品知识和 20 个内部模块，但新人日常只启用一个技能：`abao-controller`（阿豹追猎总控）。总控负责判断当前阶段，按顺序读取一个模块，最后统一检查全部结果。

```text
用户输入
   ↓
阶段 0：登记任务、产品、型号和材料
   ↓
阶段 1：需要时一次性读取完整链接集
   ↓
阶段 2：按当前产品和问题读取少量知识
   ↓
阶段 3：生成培训用假想消费者评论和回复
   ↓
阶段 4：全批统一去重、核事实、查型号和回复关系
   ↓
阶段 5：按原顺序交付最终稿和质检摘要
```

“单点出发”是指一次只推进一个当前阶段，避免产品知识、写作、采集和质检同时抢着执行。“总控”不是另一个真实的后台程序，而是一份负责调度和验收的 Skill 规则。

仓库中的 20 个原入口已经改为 `MODULE.md`，所以从仓库根目录导入时不会被 DeepSeek++ 识别成 20 个可自动触发的 Skill。不要再导入旧的 `write-consumer-comments/SKILL.md` 或 `use-pocket-knowledge/SKILL.md` 链接；它们已经改成内部模块。新人只导入总控。

---

## 二、安装前准备

### 2.1 设备要求

- 建议使用 Windows 或 macOS 电脑上的 Chrome。
- Edge 和 Firefox 也可以使用，但安装扩展的步骤略有不同。
- 需要能打开 `chat.deepseek.com`、`github.com` 和 `raw.githubusercontent.com`。
- 需要一个可正常登录的 DeepSeek 网页账号。
- GitHub 仓库目前是公开仓库，不需要 GitHub 登录才能读取；如果以后仓库改为私有，必须在 DeepSeek++ 的 GitHub 导入流程中使用它支持的登录或访问方式。

### 2.2 哪些组件暂时不需要安装

只使用侧边栏、Skill、项目上下文、保存项、网页读取和导出功能时，不需要 Shell Native Host，也不需要 Multimodal Native Host。只有在需要 DeepSeek++ 执行本机命令、运行 Python、调用 OfficeCLI 或处理输入框图片/视频时，才按扩展 `MCP` 页的提示安装对应组件。

不要为了导入本仓库而安装来路不明的脚本、扩展或“加速器”。DeepSeek++ 本体优先从 Chrome Web Store 或其 GitHub Releases 安装。

---

## 三、下载并安装 DeepSeek++

### 3.1 Chrome：推荐方式

1. 打开 DeepSeek++ 的 [Chrome Web Store 页面](https://chromewebstore.google.com/detail/deepseek++/kdmpkkahkhdmdhfkdihkopikgcocbpbf?hl=zh-CN)。
2. 点击“添加至 Chrome”。
3. 在浏览器提示中完成添加。
4. 点击 Chrome 右上角的扩展图标，把 DeepSeek++ 固定到工具栏，方便新人找到。
5. 打开 [DeepSeek 网页版](https://chat.deepseek.com) 并登录。
6. 刷新 DeepSeek 页面。正常情况下，页面右侧会出现 DeepSeek++ 侧边栏入口。
7. 打开侧边栏，先进入“设置”，确认语言选择为“简体中文”或“跟随浏览器”。

### 3.2 Edge 或 Firefox：使用 Release 包

1. 打开 [DeepSeek++ Releases](https://github.com/zhu1090093659/deepseek-pp/releases)。
2. 下载与浏览器对应的压缩包：
   - Chrome：`deepseek-plus-plus-<version>-chrome.zip`
   - Edge：`deepseek-plus-plus-<version>-edge.zip`
   - Firefox：`deepseek-plus-plus-<version>-firefox.zip`
3. 将压缩包解压到一个固定目录，例如 `D:\Tools\DeepSeek++`。不要解压后再随意移动目录，否则浏览器扩展可能失效。
4. Chrome 打开 `chrome://extensions/`；Edge 打开 `edge://extensions/`；Firefox 打开 `about:debugging#/runtime/this-firefox`。
5. Chrome 或 Edge 开启“开发者模式”，点击“加载已解压的扩展程序”，选择解压后的扩展目录。
6. Firefox 按当前 Release 说明临时载入或安装扩展。
7. 打开 DeepSeek 网页并检查右侧侧边栏。

### 3.3 安装后第一次检查

按顺序检查：

1. DeepSeek 页面能打开 DeepSeek++ 侧边栏。
2. 侧边栏能看到“设置”“Skill”“项目”或类似入口。
3. Skill 页面能打开，且页面没有持续报错。
4. 输入框输入 `/` 时能看到 Skill 自动补全或技能选择入口。

如果侧边栏没有出现，先刷新 DeepSeek 页面；仍没有时，在扩展管理页确认 DeepSeek++ 已启用，并重新打开一个 `chat.deepseek.com` 标签页。

---

## 四、从 GitHub 导入阿豹总控

### 4.1 推荐：导入仓库并只选择总控

1. 打开 DeepSeek++ 侧边栏。
2. 进入 **Skill** 页面。
3. 点击 **从 GitHub 导入**、**GitHub Import** 或同义按钮。
4. 粘贴仓库地址：

   ```text
   https://github.com/ly23055601-glitch/desktop-tutorial1
   ```

5. 点击预览或扫描。
6. 预览结果应看到一个可导入的 `abao-controller`。如果界面显示的仍是 20 个旧 Skill，说明浏览器读取的是旧版本缓存；先刷新 GitHub 导入窗口，再确认仓库分支为 `main`。
7. 只勾选 `abao-controller`，不要勾选任何旧的直接入口。
8. 点击“导入”或“Import”。
9. 在 Skill 列表中找到 `abao-controller`，打开它的启用开关。

### 4.2 备用：直接导入总控文件

如果仓库导入页面无法正确扫描目录，使用单文件地址：

```text
https://github.com/ly23055601-glitch/desktop-tutorial1/blob/main/deepseek/abao-controller/SKILL.md
```

粘贴后选择导入。总控同目录下的 `references` 会作为配套资料优先被读取。仓库中较大的产品知识不会全部塞进一次导入的提示词，而是由总控在需要的阶段按仓库路径读取，这样可以减少上下文浪费。

### 4.3 导入后正确的开关状态

新人培训建议保持以下状态：

| 项目 | 建议状态 | 原因 |
| --- | --- | --- |
| `abao-controller` | 开启 | 唯一总控入口 |
| 原来直接导入的 20 个旧入口 | 关闭或删除 | 避免多个入口同时触发、重复写稿 |
| DeepSeek++ 内置 Skill | 按任务需要 | 与本包无关，可单独使用 |
| Shell/MCP | 默认关闭 | 只有需要本机工具时才启用 |

如果以前已经导入过 `write-consumer-comments`、`use-pocket-knowledge` 等旧条目，先在 Skill 管理页面关闭它们，再启用 `abao-controller`。不要同时让共享写手、Pocket 知识和总控自动触发。

### 4.4 验证导入是否成功

在新对话输入：

```text
请检查阿豹总控是否已加载。只返回：当前技能名称、当前阶段、下一步需要的材料；不要开始写稿。
```

正确结果应能识别 `abao-controller`，并停在阶段 0，要求你提供任务和材料。若它直接开始写评论，说明旧 Skill 仍处于启用状态，或者你没有在当前对话中启用总控。

---

## 五、新人第一次任务的标准开场

新人不要只发“帮我写评论”。推荐使用下面的模板：

```text
调用阿豹总控。
任务模式：全流程
产品：DJI Pocket
型号：如果原帖没有明确，请先保持未知
目标：根据我提供的原帖材料生成培训用假想消费者评论和回复
数量：按默认培训规则
材料：下面提供正文、截图、转写和评论
要求：最后统一去重、核对产品事实、型号、回复父句和原输入覆盖

【原帖或材料开始】
在这里粘贴材料
【原帖或材料结束】
```

如果是链接而不是正文，明确告诉总控是否需要读取链接：

```text
调用阿豹总控。
任务模式：全流程
请先登记下面的完整链接集合；仅在材料不足时进入阶段 1 读取，不要逐条重复调用。
产品目标：DJI Pocket
链接：
1. https://...
2. https://...
3. https://...
最后做全批去重和统一质检。
```

如果新人只有一段产品问题，不需要写评论：

```text
调用阿豹总控。
任务模式：只查知识
产品：DJI Pocket
问题：Pocket4P 在这个拍摄场景下需要注意哪些条件？
请给出事实、条件、来源和待核项，不要生成消费者评论。
```

---

## 六、按阶段使用：单点触发命令

### 6.1 阶段 0：登记和拆任务

用途：先把任务范围说清楚，不读全部知识，不写稿。

```text
只做阶段 0：登记下面的任务。
请返回产品/型号、目标、数量、平台、材料范围、明确禁提、唯一作品列表、待补材料和下一阶段。
不要采集、不要查完整知识库、不要写评论。
```

阶段 0 的完成标志是：总控能说明有几个作品、几个产品目标、哪几项是重复输入、哪些材料真实存在、下一步做什么。

### 6.2 阶段 1：一次性读取链接

只有链接材料不足时才使用。将完整链接集合一次交给总控：

```text
只做阶段 1：读取已登记的完整链接集合。
只返回每个作品的证据范围、读取成功/失败、缺失字段和人工处理项。
不要写评论，不要补猜未读取的画面、声音或作者回复。
```

阶段 1 不负责发布、点赞、账号管理、购买服务或自动修复失败链接。失败链接保留在清单里，不能因为失败就假装已读。

### 6.3 阶段 2：单产品知识

每次只查一个产品线和一个当前问题。Pocket 示例：

```text
只做阶段 2：调用 Pocket 知识模块。
问题：独自旅行时，当前材料涉及哪些可能相关的产品事实、使用条件和待核项？
请只返回与当前材料有关的少量事实，不要写评论。
```

其他产品可替换为：

- `调用 Mobile 知识模块`
- `调用 Mic 知识模块`
- `调用 Nano 知识模块`
- `调用 Osmo 360 知识模块`

阶段 2 要区分：官方产品事实、来源中的公开原话、编辑解释、消费者假设和待核项。`pending` 或 `conflict` 不能写成肯定功能。型号不明时不要自行猜成最新款。

### 6.4 阶段 3：生成候选稿

```text
只做阶段 3：根据已登记证据和已核产品事实生成候选稿。
沿用培训用假想消费者草稿规则，绑定每条事实的来源、人物和直接父句。
不要把候选稿标记为最终稿，不要跳过阶段 4。
```

生成的是培训草稿，不是实际消费者反馈，不发布到抖音、小红书、B站或其他平台。假想人物可以有假想的使用感受，但这不能证明产品参数、功能效果或原帖设备。

### 6.5 阶段 4：统一去重和质检

```text
只做阶段 4：对本次全部候选稿统一质检。
必须跨帖、跨组、跨产品比较主评与回复，检查文本重复、意思重复、模板扎堆、产品事实、型号、证据、回复父句、人物一致性、数量和原输入覆盖。
先列问题，再定向修订；修改后重新做全批去重。不要只检查最后一组。
```

阶段 4 的重点不是把所有句子改成不同说法，而是判断是否真的在说不同的事。准确型号名、产品术语和必要条件可以重复；只换职业、地点、表情或近义词仍然表达同一个意思时，应重选关注点。

### 6.6 阶段 5：最终交付

```text
只做阶段 5：交付当前 revision 的最终稿。
先核对原输入覆盖、实际主评/回复数量、质检状态、去重改动、未解决问题和脚本是否实际运行。
正文与质检摘要分开输出，不要把内部人物标签和分析混入评论正文。
```

### 6.7 中途继续

```text
继续上次阿豹总控任务。
读取当前状态，从未完成阶段继续，不要重新采集已经成功的作品，不要重复生成已经通过的稿件。
```

如果换了新对话且总控找不到上一份状态，粘贴上次的阶段摘要、候选稿和证据清单。不要要求模型凭聊天标题恢复任务。

---

## 七、标准质检清单

新人交付前逐项检查：

### 7.1 覆盖和数量

- 每个原输入位置都有去向：交付、跳过或阻塞。
- 重复链接被合并但原位置仍可追溯。
- 不同产品目标没有被误合并。
- 主评和回复数量符合本次任务要求。
- 没有用无关泛夸补数量。

### 7.2 去重

- 同一楼没有相同句子或连续复述。
- 同一帖的主评关注点有区别。
- 不同帖没有反复使用同一个“背景＋场景＋卖点＋总结”模板。
- 只换人名、职业、地点、型号或表情的句子已重新判断。
- 准确型号名和必要产品术语的重复没有被机械删除。

### 7.3 事实和证据

- 功能、参数、兼容、价格、套装和对比结论都绑定型号、条件和来源。
- `pending/conflict` 没有写成确定能力。
- 原帖没有明确设备时，没有把目标产品当成原片设备。
- 没看见的画面、没听见的声音、没有的评论回复没有被补写。
- 假想人物经历没有被当作产品证据。

### 7.4 回复和语言

- 每条回复有明确直接父句。
- 回复没有为了给出预设答案而反造问题。
- 多个说话者的声音和关注点有区别。
- Pocket 正文和回复不使用 `Osmo`；其他产品线不继承这条限制。
- 对外正文不夹带内部分析、来源 ID、人物标签或质检分。

### 7.5 交付状态

- `pass` 表示当前版本已经完成全批检查。
- `partial` 表示有可用部分但还有缺项。
- `blocked` 表示关键材料不足，不能安全完成。
- “脚本未运行”“语义审查未完成”“用户尚未确认”不能写成“全部通过”。

---

## 八、常见问题处理

### 问题 1：导入仓库后仍显示 20 个技能

检查导入的是否是最新 `main` 分支。先删除或关闭旧导入源，再重新扫描仓库。新结构只有 `deepseek/abao-controller/SKILL.md`，原来的 20 个入口是 `MODULE.md`，不会被 DeepSeek++ 当作独立 Skill 发现。

### 问题 2：导入后总控找不到资料

检查浏览器是否能打开：

```text
https://raw.githubusercontent.com/ly23055601-glitch/desktop-tutorial1/main/MANIFEST.json
```

如果打不开，检查代理、GitHub 访问权限和浏览器网络。导入总控只会带入同目录有限的参考资料；产品知识需要在当前阶段按 raw 路径或本地仓库副本读取。

### 问题 3：DeepSeek++ 报资源过大或资源被省略

这是正常的资源限制，不代表仓库损坏。不要把整个产品知识库直接塞进 Skill。让总控按当前产品、当前问题读取索引和少量文件；如果必须离线使用，先把仓库下载到本机，再让支持本地文件的工作区读取。

### 问题 4：导入成功但输入 `/` 看不到总控

确认 Skill 开关已开启，刷新 DeepSeek 页面，并在新对话重新输入 `/`。如果仍没有，删除旧版本后重新导入单文件总控链接。

### 问题 5：模型一上来就写稿，跳过阶段

先发送：

```text
停止写稿。只返回当前阶段、active_module、已确认材料、缺失材料和下一步；不要执行下一阶段。
```

然后重新使用“只做阶段 0”。同时确认旧的直接写手 Skill 已关闭。

### 问题 6：想用 Python 质检，但提示找不到命令

不要伪造脚本通过。先完成语义质检，并在摘要写明“脚本未运行”。如果 DeepSeek++ 的 Shell Native Host 已安装且允许本机命令，再按仓库中的实际脚本路径运行；运行结果要和当前稿件 revision 对应。

### 问题 7：链接读取失败

保留失败链接和失败原因，继续处理有证据的作品。不要自动换采集方式、重复下载或把搜索摘要当成原帖。新人可以把正文、截图、转写或评论作为替代材料重新提交。

---

## 九、更新和版本管理

总控更新后，打开 DeepSeek++ 的 Skill 管理页面，找到 `abao-controller`，点击“检查更新”或“同步”。如果没有同步按钮，删除旧总控，重新导入仓库地址并选择 `abao-controller`。

更新后重新发送：

```text
请报告当前阿豹总控版本、仓库来源、已加载的配套资料和是否检测到更新。不要执行任务。
```

培训管理员每次更新应记录：

- GitHub commit 或版本号。
- 总控规则发生了什么变化。
- 产品知识资料是否变化。
- 是否重新做过一轮样例任务。
- 旧任务是否需要重新质检。

不要把用户认可的一句评论直接扩展成所有产品都适用的规则；不要把历史稿、旧质检报告或旧项目路径重新当成当前证据。

---

## 十、新人培训验收题

新人完成下面任务，视为完成基础培训：

```text
调用阿豹总控。
只做阶段 0：登记一个 Pocket 产品评论培训任务。
有 3 个输入，其中第 1 个和第 3 个是同一作品的重复链接；第 2 个只有用户提供的截图和文字，没有原始链接。
请返回唯一作品数、证据范围、产品型号的不确定性、下一阶段和不能假设的内容。
不要采集、不要查知识、不要写评论。
```

合格标准：新人能看出应保留两个唯一作品；截图和文字可以作为用户材料，但不等于完整视频证据；型号不明时不能猜成 Pocket4；阶段 0 完成后应进入阶段 1 或阶段 2，而不是直接写稿。

第二题：

```text
只做阶段 4：检查以下三条候选评论是否同义重复，并说明应保留哪条、为什么。不要只按字数判断。
```

合格标准：能区分纯文本重复、只换身份的意思重复、以及关注点确实不同的合理重复；不能用“每条都换一个卖点”代替语义判断。

---

## 十一、最终使用口诀

```text
先登记，再取证；
先按产品查少量知识，再写候选；
每次只启动一个当前模块；
全批写完后统一去重；
修改一条，也要复查受影响整楼和全批；
没有证据就标缺口，没有运行就不报通过。
```
